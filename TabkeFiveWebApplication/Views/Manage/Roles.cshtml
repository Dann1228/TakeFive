@model IEnumerable<TabkeFiveWebApplication.Controllers.RoleViewModel>

@{
    ViewBag.Title = "Roles";
}

<h2>Roles</h2>


    <button type="button" class="btn  btn-primary" onclick="location.href='@Url.Action("CreateRole", "Manage")'">新增權限</button>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
    
    </tr>
}

</table>
