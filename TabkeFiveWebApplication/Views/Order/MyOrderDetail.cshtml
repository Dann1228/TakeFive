@model IEnumerable<TabkeFiveWebApplication.Models.ViewModels.OrderDetailViewModel>
@{
    ViewBag.Title = "訂單詳細明細";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>MyOrderDetail</h2>

<table class="table table-striped table-hover table-bordered" id="example">
    <thead>
        <tr>
            <th>
                商品名稱
            </th>
            <th>
                商品價格
            </th>
            <th>
                購買數量
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BPrice)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BQty)
                </td>
            </tr>
        }
    </tbody>
</table>
@Html.ActionLink("返回訂單列表", "MyOrder", "Order", new { @class = "btn btn-default" })